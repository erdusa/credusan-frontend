/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CreditosGestionarComponent } from './creditos-gestionar.component';

describe('CreditosGestionarComponent', () => {
  let component: CreditosGestionarComponent;
  let fixture: ComponentFixture<CreditosGestionarComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ CreditosGestionarComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(CreditosGestionarComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
}); */
